// Implement the function below.
// Keep it pure: no DOM access, no network, no randomness (unless specified).
function fromCamelCase(s) {
  // TODO
}

// Do not edit below this line
window.fromCamelCase = fromCamelCase;

// Auto-run test
try { (function(){
const r=window.fromCamelCase('helloWorldTest'); const p=[],f=[]; if(r==='hello-world-test')p.push('a1'); return {passedIds:p, failedIds:f, passed:p.length===1};
})(); } catch(e){ console.error(e); }
