// Implement the function below.
// Keep it pure: no DOM access, no network, no randomness (unless specified).
function romanToInt(s) {
  // TODO
}

// Do not edit below this line
window.romanToInt = romanToInt;

// Auto-run test
try { (function(){
const fn=window.romanToInt; const p=[],f=[]; if(fn('III')===3)p.push('a1'); if(fn('IV')===4)p.push('a2'); if(fn('MCMXCIV')===1994)p.push('a3'); return {passedIds:p, failedIds:f, passed:p.length===3};
})(); } catch(e){ console.error(e); }
