// Implement the function below.
// Keep it pure: no DOM access, no network, no randomness (unless specified).
function rotateRight(arr,k) {
  // TODO
}

// Do not edit below this line
window.rotateRight = rotateRight;

// Auto-run test
try { (function(){
const out=window.rotateRight([1,2,3,4,5],2); const p=[],f=[]; if(JSON.stringify(out)===JSON.stringify([4,5,1,2,3]))p.push('a1'); return {passedIds:p, failedIds:f, passed:p.length===1};
})(); } catch(e){ console.error(e); }
