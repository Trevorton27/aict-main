// Implement the function below.
// Keep it pure: no DOM access, no network, no randomness (unless specified).
function twoSum(nums,target) {
  // TODO
}

// Do not edit below this line
window.twoSum = twoSum;

// Auto-run test
try { (function(){
const out=window.twoSum([2,7,11,15],9); const p=[],f=[]; if(Array.isArray(out)&&out[0]===0&&out[1]===1)p.push('a1'); return {passedIds:p, failedIds:f, passed:p.length===1};
})(); } catch(e){ console.error(e); }
