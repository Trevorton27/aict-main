// Implement the function below.
// Keep it pure: no DOM access, no network, no randomness (unless specified).
function toCamelCase(s) {
  // TODO
}

// Do not edit below this line
window.toCamelCase = toCamelCase;

// Auto-run test
try { (function(){
const r=window.toCamelCase('hello-world test'); const p=[],f=[]; if(r==='helloWorldTest')p.push('a1'); return {passedIds:p, failedIds:f, passed:p.length===1};
})(); } catch(e){ console.error(e); }
