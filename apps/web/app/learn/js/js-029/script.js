// Implement the function below.
// Keep it pure: no DOM access, no network, no randomness (unless specified).
function longestCommonPrefix(arr) {
  // TODO
}

// Do not edit below this line
window.longestCommonPrefix = longestCommonPrefix;

// Auto-run test
try { (function(){
const p=[],f=[]; const fn=window.longestCommonPrefix; if(fn(['flower','flow','flight'])==='fl')p.push('a1'); if(fn(['dog','racecar','car'])==='')p.push('a2'); return {passedIds:p, failedIds:f, passed:p.length===2};
})(); } catch(e){ console.error(e); }
