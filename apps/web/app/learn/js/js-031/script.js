// Implement the function below.
// Keep it pure: no DOM access, no network, no randomness (unless specified).
function difference(a,b) {
  // TODO
}

// Do not edit below this line
window.difference = difference;

// Auto-run test
try { (function(){
const r=window.difference([1,2,3,2],[2,4]); const p=[],f=[]; if(JSON.stringify(r)==='[1,3]')p.push('a1'); return {passedIds:p, failedIds:f, passed:p.length===1};
})(); } catch(e){ console.error(e); }
